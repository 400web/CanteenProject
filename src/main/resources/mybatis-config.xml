<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <typeAliases>
        <package name="com.cp.mapper"/>
        <package name="com.cp.domain"/>
    </typeAliases>
    <plugins>
        <plugin interceptor="com.cp.interceptor.MyPageInterceptor">
            <property name="dbType" value="mysql"/>
        </plugin>
    </plugins>
    <!-- 数据库环境配置 -->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <!-- 数据库连接信息 -->
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/canteen?useSSL=false&amp;autoReconnect=true&amp;useUnicode=true&amp;characterEncoding=utf8&amp;serverTimezone=GMT%2B8"/>
                <property name="username" value="root"/>
                <property name="password" value="qw1234567"/>
            </dataSource>
        </environment>
    </environments>

    <!-- 映射器（Mapper）配置 -->
    <mappers>
        <package name="com.cp.mapper"/>
    </mappers>
</configuration>

